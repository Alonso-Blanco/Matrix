{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Curso-Requisito</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Curso</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('cursoRequisito') }}">
            Volver a la lista
        </a>
    </li>
    <li>
        <a href="{{ path('cursoRequisito_edit', { 'id': entity.id }) }}">
            Editar
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
