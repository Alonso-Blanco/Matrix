{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Cursos-Requisitos</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Curso</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cursoRequisito_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('cursoRequisito_show', { 'id': entity.id }) }}">show</a>
                        <a href="{{ path('cursoRequisito_show', { 'id': entity.id }) }}">Mostrar</a>
                    </li>
                    <li>
                        <a href="{{ path('cursoRequisito_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('cursoRequisito_new') }}">
               <button class="button-new btn btn-primary">Crear nuevo Requisito</button>
            </a>
        </li>
    </ul>
    {% endblock %}
