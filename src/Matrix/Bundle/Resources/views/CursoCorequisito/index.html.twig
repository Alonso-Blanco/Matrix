{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Curso-Corrrequisito</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Curso</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cursoCorequisito_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('cursoCorequisito_show', { 'id': entity.id }) }}">Mostrar</a>
                    </li>
                    <li>
                        <a href="{{ path('cursoCorequisito_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('cursoCorequisito_new') }}">
                Crear un nuevo correquisito
            </a>
        </li>
    </ul>
    {% endblock %}
