{% extends '::base.html.twig' %}

{% block body -%}
   <h1 class="header-list">Horario</h1>

    <table class="table-list table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Dia</th>
                <td>{{ entity.dia }}</td>
            </tr>
            <tr>
                <th>Hora de Inicio</th>
                <td>{{ entity.horainicio|date("H:i") }}</td>
            </tr>
            <tr>
                <th>Hora de Fin</th>
                <td>{{ entity.horafin|date("H:i") }}</td>
            </tr>
        </tbody>
    </table>

 <ul>
        <li class="list-tb">
            <a href="{{ path('horario') }}">
                <button class="button-new btn btn-primary">Volver a la lista</button>
            </a>
        </li>
        <li class="list-tb">
                        <a href="{{ path('horario_edit', { 'id': entity.id }) }}">
                            <button class="btn btn-default">Editar</button>
                        </a>
                    </li>
       </ul>

{% endblock %}
