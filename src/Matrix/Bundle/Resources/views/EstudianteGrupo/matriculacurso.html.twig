{% extends '::base.html.twig' %}

{% block body -%}
<h1 class="header-list titulo-no-centrado">Cursos para Matricular</h1>


	<div class="list-group">
    {% for e in entity %}
		<div class="fila">
            <a href="#" class="list-group-item activable info">
              {{ e.sigla ~ ' - ' ~ e.nombreCurso }}
            </a>
            <a href="{{ path('estudiantegrupo_matriculagrupo', { 'id': e.id }) }}" class="opciones">
              <button class="list-group-item btn btn-custom">
                Matricular
              </button>
            </a>
        </div>
	{% endfor %}

</table> 
{% endblock %}