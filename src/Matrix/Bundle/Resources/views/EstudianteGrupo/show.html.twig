{% extends '::base.html.twig' %}

{% block body -%}
    <h1 class="header-list">Estudiante</h1>

    <table class="table-list table record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nota</th>
                <td>{{ entity.nota }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li class="list-tb">
        <a href="{{ path('estudiantegrupo') }}">
            <button class="button-new btn btn-primary">Volver a los Estudiante</button>
        </a>
    </li>
    <li class="list-tb">
        <a href="{{ path('estudiantegrupo_edit', { 'id': entity.id }) }}">
            <button class="button-new btn btn-primary">Editar</button>
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
